### Documented — the park payload's attraction grouping keys on the name, and that is correct

The attractions list route documented three of its surplus rows as "two
different rides carrying the same curated name", hidden by the park payload's
grouping. Resolved against the upstream entities by coordinates on 2026-09-20,
none of the three is: both `wahoo-racer` and `typhoon-twister` at Hurricane
Harbor Arlington answer for themeparks.wiki's Typhoon Twister, and Wahoo Racer
is a third row (`wahoo-racer-twisted-whizzard`) on its own coordinates. Sea
World's pair and Hurricane Harbor New Jersey's read the same way. They are the
rows §4a of the attraction-status guide describes, where one row was handed the
name another row's slug claims.

The route's Swagger description now says so, and carries the current
population: 48 surplus rows across 15 parks in 45 name groups. Measured over
all 210 parks and 7300 rows, grouping by name serves 7252 attractions and
grouping by name plus slug base serves 7255 — three rides published twice, not
three recovered. `park-integration.dedupe.spec.ts` pins the three pairs, so the
upgrade to a slug key fails the suite rather than production.
