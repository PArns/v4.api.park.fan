/**
 * Unified Crowd Level Type
 *
 * Standard 5-level crowd rating used across all park and attraction endpoints.
 * Replaces previous inconsistent 6-level enum.
 */
export type CrowdLevel = "very_low" | "low" | "moderate" | "high" | "very_high";
