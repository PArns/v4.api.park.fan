# FastAPI & Server
fastapi==0.141.1
# starlette is transitive (via fastapi) but pinned on purpose: everything below
# 1.3.1 is unpatched (PYSEC-2026-161/248/249/2280/2281) and fastapi's own floor
# (starlette>=0.46.0) would not keep us above it.
starlette==1.6.0
uvicorn[standard]==0.39.0
gunicorn==23.0.0  # process manager: runs uvicorn workers with --preload (see gunicorn.conf.py)
pydantic==2.12.5
pydantic-settings==2.11.0

# ML & Data Processing
catboost==1.2.10  # pinned: nightly retrain must not silently pick up a new major
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.6.1

# Database
psycopg2-binary==2.9.10
sqlalchemy==2.0.36

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2
psutil==6.1.1  # Memory monitoring (latest stable)
